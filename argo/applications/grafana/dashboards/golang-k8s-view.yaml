apiVersion: v1
data:
  golang-k8s-view.json: |-
    {
      "annotations": { "list": [] },
      "editable": true,
      "fiscalYearStartMonth": 0,
      "graphTooltip": 1,
      "id": null,
      "links": [],
      "liveNow": false,
      "panels": [
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "fieldConfig": { "defaults": {}, "overrides": [] },
          "gridPos": { "h": 9, "w": 6, "x": 0, "y": 0 },
          "id": 1,
          "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
          "targets": [
            {
              "expr": "histogram_quantile(0.5, sum by (le) (rate(http_server_request_body_size_bytes_bucket[5m])))",
              "legendFormat": "p50",
              "refId": "A"
            }
          ],
          "title": "Request Body Size (bytes) — p50",
          "type": "timeseries"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "fieldConfig": { "defaults": {}, "overrides": [] },
          "gridPos": { "h": 9, "w": 6, "x": 6, "y": 0 },
          "id": 2,
          "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
          "targets": [
            {
              "expr": "histogram_quantile(0.9, sum by (le) (rate(http_server_request_body_size_bytes_bucket[5m])))",
              "legendFormat": "p90",
              "refId": "A"
            }
          ],
          "title": "Request Body Size (bytes) — p90",
          "type": "timeseries"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "fieldConfig": { "defaults": {}, "overrides": [] },
          "gridPos": { "h": 9, "w": 6, "x": 12, "y": 0 },
          "id": 3,
          "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
          "targets": [
            {
              "expr": "sum(rate(http_server_request_body_size_bytes_count[5m]))",
              "legendFormat": "requests/s",
              "refId": "A"
            },
            {
              "expr": "sum(rate(http_server_request_body_size_bytes_sum[5m]))",
              "legendFormat": "bytes/s (sum rate)",
              "refId": "B"
            }
          ],
          "title": "Request Body Size — Volume & Rate",
          "type": "timeseries"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "fieldConfig": { "defaults": {}, "overrides": [] },
          "gridPos": { "h": 9, "w": 6, "x": 0, "y": 9 },
          "id": 4,
          "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
          "targets": [
            {
              "expr": "histogram_quantile(0.5, sum by (le) (rate(http_server_request_duration_seconds_bucket[5m])))",
              "legendFormat": "p50",
              "refId": "A"
            }
          ],
          "title": "Request Duration (s) — p50",
          "type": "timeseries"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "fieldConfig": { "defaults": {}, "overrides": [] },
          "gridPos": { "h": 9, "w": 6, "x": 6, "y": 9 },
          "id": 5,
          "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
          "targets": [
            {
              "expr": "histogram_quantile(0.9, sum by (le) (rate(http_server_request_duration_seconds_bucket[5m])))",
              "legendFormat": "p90",
              "refId": "A"
            }
          ],
          "title": "Request Duration (s) — p90",
          "type": "timeseries"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "fieldConfig": { "defaults": {}, "overrides": [] },
          "gridPos": { "h": 9, "w": 6, "x": 12, "y": 9 },
          "id": 6,
          "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
          "targets": [
            {
              "expr": "sum(rate(http_server_request_duration_seconds_count[5m]))",
              "legendFormat": "requests/s",
              "refId": "A"
            },
            {
              "expr": "sum(rate(http_server_request_duration_seconds_sum[5m]))",
              "legendFormat": "seconds/s (sum rate)",
              "refId": "B"
            }
          ],
          "title": "Request Duration — Volume & Sum Rate",
          "type": "timeseries"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "fieldConfig": { "defaults": {}, "overrides": [] },
          "gridPos": { "h": 9, "w": 6, "x": 0, "y": 18 },
          "id": 7,
          "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
          "targets": [
            {
              "expr": "histogram_quantile(0.5, sum by (le) (rate(http_server_response_body_size_bytes_bucket[5m])))",
              "legendFormat": "p50",
              "refId": "A"
            }
          ],
          "title": "Response Body Size (bytes) — p50",
          "type": "timeseries"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "fieldConfig": { "defaults": {}, "overrides": [] },
          "gridPos": { "h": 9, "w": 6, "x": 6, "y": 18 },
          "id": 8,
          "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
          "targets": [
            {
              "expr": "histogram_quantile(0.9, sum by (le) (rate(http_server_response_body_size_bytes_bucket[5m])))",
              "legendFormat": "p90",
              "refId": "A"
            }
          ],
          "title": "Response Body Size (bytes) — p90",
          "type": "timeseries"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
          "fieldConfig": { "defaults": {}, "overrides": [] },
          "gridPos": { "h": 9, "w": 6, "x": 12, "y": 18 },
          "id": 9,
          "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
          "targets": [
            {
              "expr": "sum(rate(http_server_response_body_size_bytes_count[5m]))",
              "legendFormat": "responses/s",
              "refId": "A"
            },
            {
              "expr": "sum(rate(http_server_response_body_size_bytes_sum[5m]))",
              "legendFormat": "bytes/s (sum rate)",
              "refId": "B"
            }
          ],
          "title": "Response Body Size — Volume & Rate",
          "type": "timeseries"
        }
      ],
      "refresh": "10s",
      "schemaVersion": 38,
      "style": "dark",
      "tags": ["otel", "gin"],
      "templating": {
        "list": [
          {
            "current": { "selected": false, "text": "Prometheus", "value": "${DS_PROMETHEUS}" },
            "hide": 0,
            "name": "DS_PROMETHEUS",
            "options": [],
            "query": "prometheus",
            "type": "datasource"
          }
        ]
      },
      "time": { "from": "now-30m", "to": "now" },
      "timepicker": {},
      "timezone": "",
      "title": "OTel Gin HTTP Server Metrics",
      "uid": "otel-gin-http-restricted-no-service-filter"
    }
kind: ConfigMap
metadata:
  labels:
    app: golang-k8s-view-grafana
    app.kubernetes.io/instance: golang-k8s-view
    app.kubernetes.io/managed-by: Helm
    prometheus: golang-k8s-view-grafana
    grafana_dashboard: "1"
    heritage: Helm
    release: golang-k8s-view
  name: golang-k8s-view
  namespace: argocd